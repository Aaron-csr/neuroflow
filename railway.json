{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm run clean && npm ci --omit=dev && npm run build",
    "outputDirectory": "dist"

  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
